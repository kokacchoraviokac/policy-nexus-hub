
import React from 'react';
import { Routes, Route } from 'react-router-dom';
import Router from './index';

const AppRoutes: React.FC = () => {
  // Use the Router component directly
  return <Router />;
};

export default AppRoutes;
